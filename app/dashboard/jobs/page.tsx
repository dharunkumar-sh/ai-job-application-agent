import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Jobs — JobBuddy AI",
};

export default function JobsPage() {
  return (
    <div className="h-full flex flex-col items-center justify-center border-2 border-dashed border-[#23232b] rounded-3xl p-12 text-center">
      {/* Blank page for future Jobs implementation */}
    </div>
  );
}
